server:
  port: 8080

spring:
  application:
    name: springai-alibaba

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always 